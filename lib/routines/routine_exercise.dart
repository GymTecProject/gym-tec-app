import 'package:flutter/material.dart';

class RoutineExercise extends StatelessWidget {
  const RoutineExercise({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return RoutineExercise();
  }
}