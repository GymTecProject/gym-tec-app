import 'package:flutter/material.dart';

class ContextSeparator extends StatelessWidget {
  const ContextSeparator({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: 20);
  }
}
